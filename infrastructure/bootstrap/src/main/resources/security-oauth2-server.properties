spring.security.oauth2.resourceserver.jwt.issuer-uri=${RESOURCE_SERVER_AUTH_SERVER_URL}
spring.security.oauth2.resourceserver.jwt.audience=${RESOURCE_SERVER_AUTH_AUDIENCE}
spring.security.oauth2.resourceserver.jwt.realm=${RESOURCE_SERVER_AUTH_REALM}
